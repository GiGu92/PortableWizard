<Window x:Class="PortableWizard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="MainWindow" Height="500" Width="700">


   




    <xctk:Wizard x:Name="Wizard" FinishButtonClosesWindow="True">
        <xctk:WizardPage x:Name="IntroPage" 
                         Title="Welcome to Portable Wizard!"
                         Description="This Wizard will walk you though initializing your portable applications!"
                         NextButtonVisibility="Hidden"
                         BackButtonVisibility="Hidden"
                         HelpButtonVisibility="Hidden">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>                

                <Button x:Name="InitializeButton" Grid.Column="0" Grid.Row="0" Click="InitializeButton_Click">Initialize</Button>
                <Button x:Name="UninstallButton" Grid.Column="0" Grid.Row="1" Click="UninstallButton_Click">Uninstall</Button>
                <Button x:Name="ExitButton" Grid.Column="0" Grid.Row="2" Click="ExitButton_Click">Exit</Button>
                
                <Label Grid.Column="1" Grid.Row="0">Initialize your portable applications</Label>
                <Label Grid.Column="1" Grid.Row="1">Uninstall your initialized portable applications</Label>
                <Label Grid.Column="1" Grid.Row="2">Exit program</Label>
                
            </Grid>
            
        </xctk:WizardPage>
        
        
        
        <xctk:WizardPage x:Name="AppChooser" PageType="Interior"
                         Title="Which apps do you want to initialize?"
                         Description="Please choose the folder in which you store your portable apps, then select which of those you want to initialize!"
                         NextPage="{Binding ElementName=Page2}"
                         PreviousPage="{Binding ElementName=IntroPage}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <GroupBox Header="Portable applications folder location" Grid.Column="0" Grid.Row="0">
                    <StackPanel Orientation="Horizontal">
                        <TextBox x:Name="AppsPathTextBox" Width="600" TextChanged="AppsPathTextBox_TextChanged"/>
                        <Button x:Name="AppsPathBrowseButton" Click="AppsPathBrowseButton_Click">Browse</Button>
                    </StackPanel>
                </GroupBox>


                <ListBox Margin="12,83,12,12"
                        Name="AppListBox"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        Grid.Row="1">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <!-- In the video, this is in a UserControl. That's still ok to do. -->
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="35" />
                                    <ColumnDefinition Width="50" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <CheckBox
	                                HorizontalAlignment="Left"
	                                Margin="10,10,0,0"
	                                VerticalAlignment="Top"
	                                IsChecked="{Binding Path=WillProcessed}"
                                    Grid.Column="0"
                                    />
                                
                                <Image Source="{Binding Path=Icon}"
                                    Height="32"
                                    Width="32"
                                    VerticalAlignment="Top"
                                    Grid.Column="1"/>
                                <TextBlock Text="{Binding Path=Name}"
                                    Grid.Column="2" />

                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>


            </Grid>
            
        </xctk:WizardPage>




        <xctk:WizardPage x:Name="Page2" PageType="Interior"
                         Title="Page 2"
                         Description="This is the second page in the process">
            <TextBox IsReadOnly="True">Anyad</TextBox>
        </xctk:WizardPage>
        <xctk:WizardPage x:Name="LastPage" PageType="Interior"
                         Title="Last Page"
                         Description="This is the last page in the process"
                         CanFinish="True"/>
    </xctk:Wizard>
    
</Window>
